adl: "1.0"

id: scheduled-watchdog
name: Scheduled Watchdog
description: Auto-prompt agent suitable for recurring schedules (configure in Customize → Schedules)
promptMode: auto
defaultPrompt: Check the workspace for issues and summarize anything that needs attention.

harness:
  type: claude-code
  sandbox: none
