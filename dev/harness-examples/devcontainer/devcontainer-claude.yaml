adl: "1.0"
id: devcontainer-claude
name: Devcontainer Claude
description: Claude Code running in a nui-managed dev container (no user devcontainer.json required).
version: 0.1.0

harness:
  type: devcontainer
  innerHarness: claude-code

steps:
  - name: chat
    systemPrompt: You are a helpful coding assistant.
