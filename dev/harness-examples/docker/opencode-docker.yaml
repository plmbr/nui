adl: "1.0"
id: opencode-docker
name: OpenCode Docker
description: opencode running inside a Docker container (nui-opencode:latest)
harness:
  type: opencode
  sandbox: docker
  image: nui-opencode:latest
